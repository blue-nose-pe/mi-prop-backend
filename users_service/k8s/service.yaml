apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: backend
spec:
  type: ClusterIP   # Solo interno — al exterior se llega por el Gateway.
  selector:
    app: users-service
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
