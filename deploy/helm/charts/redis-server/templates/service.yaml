apiVersion: v1
kind: Service
metadata:
  name: {{ include "redisServer.fullname" . }}
  labels:
    {{- include "redisServer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: redis
      port: {{ .Values.service.port }}
      targetPort: 6379
      protocol: TCP
  selector:
    {{- include "redisServer.selectorLabels" . | nindent 4 }}
