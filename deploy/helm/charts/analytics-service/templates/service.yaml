apiVersion: v1
kind: Service
metadata:
  name: {{ include "analyticsService.fullname" . }}
  labels:
    {{- include "analyticsService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "analyticsService.selectorLabels" . | nindent 4 }}
