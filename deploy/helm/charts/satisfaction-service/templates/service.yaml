apiVersion: v1
kind: Service
metadata:
  name: {{ include "satisfactionService.fullname" . }}
  labels:
    {{- include "satisfactionService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "satisfactionService.selectorLabels" . | nindent 4 }}
