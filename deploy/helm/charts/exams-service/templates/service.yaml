apiVersion: v1
kind: Service
metadata:
  name: {{ include "examsService.fullname" . }}
  labels:
    {{- include "examsService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "examsService.selectorLabels" . | nindent 4 }}
