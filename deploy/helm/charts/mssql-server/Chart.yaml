apiVersion: v2
name: mssql-server
description: SQL Server 2022 in-cluster (StatefulSet) para staging cuando Azure SQL no se puede provisionar. Reemplazado por Azure SQL gestionado en producción.
type: application
version: 0.1.0
appVersion: "2022-latest"
