apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "usersService.fullname" . }}-spc
  labels:
    {{- include "usersService.labels" . | nindent 4 }}
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "true"
    clientID: {{ .Values.global.keyVault.userAssignedIdentityID | quote }}
    keyvaultName: {{ .Values.global.keyVault.name | quote }}
    tenantId: {{ .Values.global.keyVault.tenantId | quote }}
    objects: |
      array:
        - |
          objectName: users-service-sql-password
          objectType: secret
          objectAlias: sql-password
        - |
          objectName: users-service-redis-password
          objectType: secret
          objectAlias: redis-password
        - |
          objectName: users-service-jwt-secret
          objectType: secret
          objectAlias: jwt-secret
  secretObjects:
    - secretName: {{ include "usersService.fullname" . }}-kv
      type: Opaque
      data:
        - objectName: sql-password
          key: sql-password
        - objectName: redis-password
          key: redis-password
        - objectName: jwt-secret
          key: jwt-secret
